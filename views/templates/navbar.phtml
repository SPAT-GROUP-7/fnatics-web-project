<!-- Top Navbar Start -->
<nav class="navbar navbar-light fixed-top bg-light flex-md-nowrap p-0 border-b">
    <a class="navbar-brand col-sm-3 col-md-2 mr-0" href="#"><img src="https://lever-client-logos.s3.amazonaws.com/a26ea84a-d8a3-4394-9927-2b0243d2df5a-1552380484471.png" style="max-height: 40px; padding: 5px;" /></a>
    <ul class="navbar-nav px-3">
        <li class="navbar-item text-nowrap">
            <?php if (isset($_SESSION["loggedIn"])) : ?>
                <a class="nav-link n-text" href="logout.php">Logout</a>
            <?php else : ?>
                <a class="nav-link n-text" href="#" data-toggle="modal" data-target="#loginModal">Login</a>
            <?php endif; ?>
        </li>
    </ul>
</nav>
<!-- Top Navbar End -->

<!-- Sidebar Start -->
<div class="f-wrapper" id="wrapper">
    <div class="sidebar-wrapper">
        <ul class="sidebar-nav mx-auto my-auto px-4">
            <li>
                <a href="rota.php" class="n-text"><i class="far fa-calendar-alt"></i> Rota</a>
            </li>
            <?php if (isset($_SESSION["loggedIn"]) && ($_SESSION["loggedIn"] == true)) : ?>
                <li>
                    <a href="#" class="n-text"><i class="fas fa-handshake"></i> Shift Swap</a>
                </li>
                <li>
                    <?php if (isset($_SESSION["isAdmin"]) && ($_SESSION["isAdmin"] == true)) : ?>
                        <a href="adminPanel.php" class="n-text"><i class="fas fa-shield-alt"></i> Admin Panel</a>
                    <?php endif; ?>
                </li>
                <li>
                    <a href="#" class="n-text"><i class="fas fa-cog"></i> Settings</a>
                </li>
            <?php endif; ?>
        </ul>
    </div>
    <!-- Sidebar End -->

