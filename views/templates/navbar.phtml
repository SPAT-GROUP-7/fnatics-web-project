<!-- Top Navbar Start -->
<nav class="navbar navbar-expand-xxl navbar-light fixed-top bg-light p-0 border-b">

    <a class="navbar-brand col-sm-3 col-md-2 mr-0" href="index.php"><img src="/assets/img/logo.png" style="max-height: 40px; padding: 5px;" /></a>

    <div class="collapse navbar-collapse">
        <ul class="navbar-nav ml-auto mr-3 mt-2 mt-lg-0">
            <li class="nav-item text-nowrap mr-2">
                <?php if (isset($_SESSION["loggedIn"])) : ?>
                    <a class="nav-link n-text" href="logout.php">Logout</a>
                <?php else : ?>
                    <a class="nav-link n-text" href="#" data-toggle="modal" data-target="#loginModal">Login</a>
                <?php endif; ?>
            </li>
        </ul>
    </div>

    <button class="navbar-toggler mr-3" type="button" data-toggle="collapse" data-target="#mobileNav">
        <span class="navbar-toggler-icon"></span>
    </button>
</nav>
<!-- Top Navbar End -->

<div class="collapse" id="mobileNav">
    <div class="h-100 w-100 bg-primary">
        <button type="button" class="close mr-4 mt-3" data-toggle="collapse" data-target="#mobileNav">
            <span class="text-white"><i class="fas fa-times fa-2x"></i></span>
        </button>
        <div class="h-100 w-100 d-flex">
            <div class="mobile-content mx-auto my-auto">
                <li>
                    <a href="
                        <?php if ($_SESSION["isAdmin"] == true) { echo 'rotaDisplay.php'; } else { echo 'index.php'; } ?>
                    " class="m-text"><i class="far fa-calendar-alt"></i> Rota</a>
                </li>
                <?php if (isset($_SESSION["loggedIn"]) && ($_SESSION["loggedIn"] == true)) : ?>
                    <?php if (isset($_SESSION["isAdmin"]) && ($_SESSION["isAdmin"] == true)) : ?>
                        <li>
                            <a href="adminPanel.php" class="m-text"><i class="fas fa-shield-alt"></i> Admin Panel</a>
                        <li>
                        <li>
                            <a href="rota.php" class="m-text"><i class="fas fa-pencil-alt"></i> Edit Rota</a>
                        <li>
                    <?php endif; ?>
                        <a href="logout.php" class="m-text"><i class="fas fa-sign-out-alt"></i> Logout</a>
                    </li>
                <?php else : ?>
                    <li>
                        <a href="#" class="m-text" data-toggle="modal" data-target="#loginModal"><i class="fas fa-sign-in-alt"></i> Login</a>
                    </li>
                <?php endif; ?>
            </div>
        </div>
    </div>
</div>

<!-- Sidebar Start -->
<div class="f-wrapper d-flex" id="wrapper">
    <div class="sidebar-wrapper">
        <ul class="sidebar-nav px-4">
            <li>
                <a href="
                        <?php if (isset($_SESSION["isAdmin"]) && ($_SESSION["isAdmin"] == true)) { echo 'rotaDisplay.php'; } else { echo 'index.php'; } ?>
                    " class="n-text"><i class="far fa-calendar-alt"></i> Rota</a>
            </li>
            <?php if (isset($_SESSION["loggedIn"]) && ($_SESSION["loggedIn"] == true)) : ?>
                <?php if (isset($_SESSION["isAdmin"]) && ($_SESSION["isAdmin"] == true)) : ?>
                    <li>
                        <a href="adminPanel.php" class="n-text"><i class="fas fa-shield-alt"></i> Admin Panel</a>
                    <li>
                    <li>
                        <a href="rota.php" class="n-text"><i class="fas fa-pencil-alt"></i> Edit Rota</a>
                    <li>
                <?php endif; ?>
            <?php endif; ?>
        </ul>
    </div>
    <!-- Sidebar End -->

