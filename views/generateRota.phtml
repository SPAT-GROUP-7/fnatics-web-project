<?php require_once ("templates/header.phtml") ?>
<?php require_once ("templates/navbar.phtml") ?>

<?php
$r = $view->rotas;
?>
<div class="content-wrapper">
    <div class="container-fluid">
        <div class="row">
            <div class="col-lg-12">
                <h1>Temporary Rota</h1>
            </div>
            <form action="generateRota.php" method="POST">
                <div class="form-group">
                    <input class="form-control" type="text" name="num">
                </div>

                <div class="form-group">
                    <input type="submit" class="btn btn-danger" value="Generate a new Rota">
                </div>
            </form>
            <div class="col-lg-12">
                <form action="publishRota.php" method="POST">
                    <input type="hidden" name="rotas" value="<?php echo json_encode($r) ?>>">
                    <input type="submit" class="btn btn-primary" value="Publish Rotas">
                </form>
            </div>

            <div class="col-lg-12">
                <div class="card card-body bg-light mt-5">
                    <h1 class="card-title">Rota</h1>

                    <table class="table">
                        <tr>
                            <th>From</th>
                            <th>To</th>
                            <th>Dev A</th>
                            <th>Dev B</th>
                        </tr>
                        <?php
                        foreach ($view->rotas as $rota) {
                            ?>
                            <tr>
                                <th><?php echo $rota->getFrom() ?></th>
                                <th><?php echo $rota->getTo() ?></th>
                                <th><?php echo $rota->getDevA()->getUsername()?></th>
                                <th><?php echo $rota->getDevB()->getUsername()?></th>
                            </tr>
                            <?php
                        }
                        ?>
                    </table>
                </div>
            </div>
        </div>
    </div>
</div>


<?php require_once ("templates/footer.phtml") ?>
